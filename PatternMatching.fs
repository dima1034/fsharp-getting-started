module PatternMatching

// is a ubiquitous tool, where values matched across an equation based on structural equality

// branch expression:
// match ... with
// | ... -> 
// | ... ->

// branch expression
// with predicates:
// match ... with
// | _ when i % 3 -> ""
// | _ when i % 5 -> ""
// | _ -> string i //cast to some type

